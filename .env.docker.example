# Docker Environment Configuration
# Copy this file to .env.docker for production deployment
#
# IMPORTANT: Generate a secure BETTER_AUTH_SECRET before deploying!
# Run: openssl rand -base64 32

# ============================================================================
# REQUIRED - Better-auth Configuration
# ============================================================================
# Must be at least 32 characters - CHANGE THIS IN PRODUCTION!
# The docker-compose.yml will fail to start if this is not set
BETTER_AUTH_SECRET=

# Public URL where the app will be accessible
# For production, use your actual domain: https://transcriber.yourdomain.com
BETTER_AUTH_URL=http://localhost:3000

# ============================================================================
# OPTIONAL - Server Configuration
# ============================================================================
# Port mapping (host:container)
# Change the host port if 3000 is already in use: PORT=8080
PORT=3000
